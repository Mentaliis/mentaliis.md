{
  "name": "mentaliis",
  "private": true,
  "version": "0.1.13",
  "description": "Mentaliis — un second cerveau spatial.",
  "scripts": {
    "dev": "tauri dev",
    "test": "npm --prefix frontend run test",
    "engine:build": "node scripts/build-engine.mjs",
    "build": "node scripts/build-engine.mjs && tauri build",
    "manifest": "node scripts/build-manifest.mjs",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  }
}
